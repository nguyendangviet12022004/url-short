# user service
apiVersion: v1
kind: Service
metadata:
  name: {{ include "url-short-cluster.fullname" . }}-user-service
spec:
  selector:
    {{- include "url-short-cluster.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: user-pod
  ports:
    - protocol: TCP
      port: {{ .Values.port.userPod }}
      targetPort: {{ .Values.port.userPod }}

#url service
apiVersion: v1
kind: Service
metadata:
  name: {{ include "url-short-cluster.fullname" . }}-url-service
spec:
  selector:
    {{- include "url-short-cluster.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: url-pod
  ports:
    - protocol: TCP
      port: {{ .Values.port.urlPod }}
      targetPort: {{ .Values.port.urlPod }}
